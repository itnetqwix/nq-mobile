/** Debounce delay before search hits the network. */
export const SEARCH_API_DEBOUNCE_MS = 350;

/** Debounce delay for local list filtering (chats, FAQ, timezone picker). */
export const SEARCH_LOCAL_DEBOUNCE_MS = 220;

/** Throttle interval while scrubbing video progress. */
export const VIDEO_SEEK_THROTTLE_MS = 80;

/** Minimum gap between duplicate submit taps. */
export const SUBMIT_GUARD_MS = 1200;
