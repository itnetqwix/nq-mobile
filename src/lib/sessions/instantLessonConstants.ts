/** Instant-lesson timing — keep in sync with `nq-backend` `config/instantLesson.ts`. */
export const INSTANT_ACCEPT_WINDOW_MS = 60 * 60 * 1000;
export const INSTANT_JOIN_AFTER_ACCEPT_MS = 15 * 60 * 1000;
