/** Top coaches shown before "Show more" on trainee dashboard. */
export const TRAINEE_COACH_PREVIEW_COUNT = 12;

/** Server page size — matches preview step for predictable pagination at scale. */
export const TRAINEE_COACH_PAGE_SIZE = 12;
